// PhishGuard Frontend Environment Configuration
// This file sets the API base URL for connecting to the backend

const config = {
  API_BASE_URL: 'http://localhost:8000',
  APP_NAME: 'PhishGuard',
  APP_VERSION: '1.0.0',
  NODE_ENV: 'development'
}

module.exports = config

